{
  "name": "ap-art-history-12-25",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AP Art History image recall and quiz practice for works 12–25",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=20" }
}
